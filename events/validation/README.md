# Event validation

Where an extractor's output is checked against annotated ground truth (UAV-SEAD), with
the agreement reported per class rather than as a single score.

Empty until an extractor exists. See [`../extractors/README.md`](../extractors/README.md).
